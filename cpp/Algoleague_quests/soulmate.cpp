#include<bits/stdc++.h>

using namespace std;
using ll = long long;

void solve(){
    int n,p;
    cin>>n>>p;
    map<int,int>m;
    for(int i=0 ; i<n ; i++){
        ll temp;
        cin>>temp;
        m[i] = temp;
    }
    for(ll i=0 ; i<n ; i++){
        ll a = p
    }
}