#include<bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[]){
    string s;
    cin>>s;
    stack<char>st;
    for(auto k:s){
        if(k=='(' ||k=='['||k=='{')st.push(k);
    }
    st.empty
    return 0;
}
